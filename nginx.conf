server {
    listen 8080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This is crucial for React/Vite SPAs to handle client-side routing
        try_files $uri $uri/ /index.html;
    }
}